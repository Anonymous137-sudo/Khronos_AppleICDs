#!/bin/sh
set -eu

PATH="/opt/homebrew/bin:/opt/homebrew/sbin:/usr/local/bin:/usr/local/sbin:/usr/bin:/bin:/usr/sbin:/sbin:$PATH"

echo "OpenGLKHR installer: the default Khronos GL/EGL mode installs only under its user-space prefix."
echo "No SIP or Authenticated Root check is needed unless legacy-system mode is selected later."
